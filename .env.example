JUDGE_MODEL=claude-sonnet-4-6
ANTHROPIC_API_KEY=
# Prefer: eval "$(vault env)"  (see run_judge.sh / run_full_judge.sh)
# Optional OpenAI path (only if JUDGE_MODEL is an OpenAI model string):
# OPENAI_API_KEY=
